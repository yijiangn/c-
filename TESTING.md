# 关键功能验收清单

## 自动测试

- `StatsCalculatorTest`：正时长聚合、今日/本周/本月、连续学习、科目时长、本周完成率。

## 人工验收

1. 首次启动出现英语、数学、编程、阅读、其他 5 个科目，任务为空。
2. 添加空名称任务时不允许保存；合法任务重启 App 后仍存在。
3. 点击状态环可完成/取消完成；长按可编辑或进入二次确认删除。
4. 删除已有计时历史的任务后，统计时长不减少，日历仍显示历史时段。
5. 正计时启动后锁屏 2 分钟，回到 App 的计时与真实时间一致。
6. 番茄钟暂停后等待，暂停时间不计入学习时长；继续后正常累加。
7. 结束不足 1 分钟的计时提示不保存；大于等于 1 分钟写入记录并累加任务实际时长。
8. 日历有记录日期显示圆点；可选择历史日期添加任务、补记时长、修改或删除记录。
9. 统计页所有数值在空数据库为 0，添加记录后来自 Room 实际聚合，无预置演示数据。
10. 有连续两天正时长记录时连续学习为 2 天；当天无记录时为 0 天。
11. 删除有任务的自定义科目时提示移动到“其他”；内置科目不提供删除按钮。
12. JSON 导出后可使用覆盖和合并两种方式恢复；损坏 JSON 显示中文错误且不改数据库。
13. CSV 使用 Excel/WPS 打开时中文正常，包含日期、任务、科目、分钟和起止时间。
14. 清除全部数据需要两次确认，完成后恢复内置科目和默认设置。
15. 切换浅色、深色、跟随系统后立即生效，旋转屏幕与重启 App 后仍保留。
16. 断开网络后任务、计时、日历、统计、设置、备份与恢复全部可用。
